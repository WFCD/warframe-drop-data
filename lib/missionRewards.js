module.exports = function($) {
    const missionRewardsTable = $("#missionRewards").next("table")

    const tbody = content.children()['0']

    let location = null
    let rotation = null

    for(let tr in tbody.children) {

    }
}

function parseLocation(str) {
    const locationRegex = /([A-z]*)\/([A-z]*)\s\([A-z]*\)/i
}

function parseRotation(str) {
    const rotationRegex = /Rotation\s([A-D])/i
}